import {firstName, lastName} from "./random-name";

const initialize= function initFunction(){
    document.getElementById('name-form').addEventListener('submit', (event)=>{
        event.preventDefault();
        
    })
}